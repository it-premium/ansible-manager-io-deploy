[Unit]
After=network.target

[Service]
LimitNOFILE=1048576
ExecStart=/usr/bin/mono {{manager_path}}/ManagerServer.exe -port 8080 -path {{manager_data}}
Restart=on-failure
StartLimitInterval=600

[Install]
WantedBy=multi-user.target
